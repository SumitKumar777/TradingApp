{
	"name": "@repo/httpbackend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc -b ",
		"dev": "tsc -b && nodemon ./dist "
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@repo/db": "workspace:*",
		"@types/express": "^5.0.3",
		"express": "^5.1.0"
	},
	"devDependencies": {
		"@repo/typescript-config": "workspace:*",
    "@repo/types":"workspace:*",
		"nodemon": "^3.1.10"
	}
}